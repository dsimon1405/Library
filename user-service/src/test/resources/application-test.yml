spring:
  datasource:
    url: jdbc:mysql://localhost:${LIBRARY_DB_PORT:3306}/library_user_db_test?createDatabaseIfNotExist=true
    username: ${LIBRARY_DB_USERNAME:root}
    password: ${LIBRARY_DB_PASSWORD:1qazxsw2}

  jpa:
    hibernate:
      ddl-auto: create-drop
  #    show-sql: true
  #    properties:
  #      hibernate:
  #        format_sql: true

  cloud:
    discovery:
      enabled: false
  autoconfigure:
    exclude:
      - org.springframework.cloud.client.discovery.simple.SimpleDiscoveryClientAutoConfiguration
      - org.springframework.cloud.netflix.eureka.EurekaClientAutoConfiguration